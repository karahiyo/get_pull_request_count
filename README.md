get pullrequest count
=====================

Quick Start
-----------

setup
```
$ python3 -m venv .venv
$ source .venv/bin/activate
$ pip install -r requirements.txt
```

create config.ini
see config.ini.dummy

and run script

```
$ python get_pullrequest_count.py
```

